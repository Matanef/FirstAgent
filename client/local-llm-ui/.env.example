# Frontend environment variables (Vite)
# Copy this file to .env and fill in your values

# API key — must match AGENT_API_KEY in the backend .env
VITE_AGENT_API_KEY=your-secret-key-here

# (Optional) Backend URL override — defaults to http://localhost:3000
# VITE_API_URL=http://localhost:3000
