# FirstAgent


## Tools

- **spotify** (`server/tools/spotify.js`): A fully functional Spotify controller using the Spotify Web API and OAuth2 refresh token flow.

- **chartGenerator** (`server/tools/chartGenerator.js`): Generates SVG or HTML charts from generic JSON data.
